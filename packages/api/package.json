{
  "name": "@250-500/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint targets yet (Phase 2)' && exit 0",
    "test": "echo 'no tests yet (Phase 2)' && exit 0",
    "build": "echo 'no build yet (Phase 2)' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
